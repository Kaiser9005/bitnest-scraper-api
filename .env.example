# BitNest Scraper API - Environment Configuration
# Copy this file to .env and configure your values

# ===== RAILWAY CONFIGURATION (REQUIS pour déploiement non-interactif) =====
# Créez tokens sur: https://railway.app/account/tokens
RAILWAY_TOKEN=your-railway-token-here
RAILWAY_PROJECT_ID=your-project-id-here

# ===== SERVER CONFIGURATION =====
PORT=8080
NODE_ENV=production
LOG_LEVEL=warn                    # warn au lieu de info (MODE ÉCONOMIQUE)

# ===== SECURITY - REQUIRED =====
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Configure the SAME key in Google Apps Script
BITNEST_API_KEY=your-secret-api-key-here

# ===== RATE LIMITING (MODE ÉCONOMIQUE - Budget $5) =====
RATE_LIMIT_WINDOW_MS=3600000      # 1 hour in milliseconds
RATE_LIMIT_MAX_REQUESTS=30        # 30 requests/hour (MODE ÉCONOMIQUE - était: 60)

# ===== CACHE CONFIGURATION (MODE ÉCONOMIQUE) =====
CACHE_TTL_MS=1800000              # 30 minutes (MODE ÉCONOMIQUE - était: 300000 = 5 min)

# ===== PLAYWRIGHT CONFIGURATION (Optimisé) =====
PLAYWRIGHT_TIMEOUT_MS=20000       # 20s navigation (MODE ÉCONOMIQUE - était: 30s)
PLAYWRIGHT_WAIT_MS=3000           # 3s wait for JS (MODE ÉCONOMIQUE - était: 5s)

# ===== RETRY CONFIGURATION (Réduit pour économie) =====
MAX_RETRIES=2                     # 2 attempts (MODE ÉCONOMIQUE - était: 3)
RETRY_DELAY_MS=1500               # 1.5s delay (MODE ÉCONOMIQUE - était: 2s)

# ===== TELEGRAM CONFIGURATION (Dual-Source Validation) =====
# Get API credentials from https://my.telegram.org/apps
TELEGRAM_API_ID=27770103
TELEGRAM_API_HASH=98d3cb6185cfed3f106520d44291b4bb

# Session string - Generate with: npm run setup:telegram
# Or reuse existing session from telegram-mcp-server/.env
TELEGRAM_SESSION_STRING=your-telegram-session-string-here

# BitnestMonitor channel username (without @)
TELEGRAM_BITNEST_CHANNEL=BitnestMonitor
